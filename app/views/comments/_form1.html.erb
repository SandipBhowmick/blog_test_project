
	<%= form_for(:comment, :url => {:action => 'update', :id => @comment.id},:method => "patch") do |f| %>
		<%= f.label :name %><br>
		<%= f.text_field :name %><br>
		<br>
		<%= f.label :body %><br>
		<%= f.text_area :body %><br>
		<br>
		<%= f.submit %>
	<% end %>
